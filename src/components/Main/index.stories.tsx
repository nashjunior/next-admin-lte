import React from 'react';
import Main from '.';

// eslint-disable-next-line import/no-anonymous-default-export
export default {
  title: 'Main',
  componenet: Main,
};

// eslint-disable-next-line no-undef
export const Basic = (): JSX.Element => <Main />;
